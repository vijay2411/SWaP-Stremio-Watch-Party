{
  "name": "swap-stremio-watch-party",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "One userscript for Stremio watch parties, chat and opt-in video calls.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "demo": "node scripts/dev.mjs"
  },
  "dependencies": {
    "peerjs": "1.5.5"
  },
  "devDependencies": {
    "esbuild": "0.25.12",
    "peer": "1.0.2"
  },
  "overrides": {
    "qs": "6.16.0"
  }
}
